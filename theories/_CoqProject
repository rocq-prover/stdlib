-R . Stdlib
# When editing below lines, keep the theories/dune file in sync
# to handle when requiring Rocq >= 9.2
-arg -w -arg -register-all
-arg -w -arg -scheme-rewriting
# to handle when requiring Rocq >= 9.3
-arg -w -arg -non-boolean-if
